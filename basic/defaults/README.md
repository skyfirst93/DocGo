# Default things about go Code 

- go code is formated with tab in instead of white spaces 

- Go comipler added a ";" after every code line. That's why go functions have "{" in the same line as the function definations 

 
